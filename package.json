{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "SET NODE_ENV=development && nodemon cluster-server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node cluster-server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@skywritermd/socketredis": "^1.0.3",
    "@socket.io/sticky": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "redis": "^3.0.2",
    "socket.io": "^3.1.0",
    "socket.io-emitter": "^3.2.0",
    "socket.io-redis": "^6.1.0"
  }
}
